var game = game || {};

(function(game, createjs) {
	// function Enemy(point, targetX, targetY) {
	// 	this.initialize(point, targetX, targetY);
	// }

	// Enemy.prototype = new createjs.Container();

	// Enemy.prototype.parent_initialize = Enemy.prototype.initialize;

	// Enemy.prototype.initialize = function() {
	// 	var
	// 		width = 10,
	// 		height = 20,
	// 		body =  game.helpers.rect(width, height, 'rbga(255, 0, 0, 1)')
	// 		;

	// 	// this.vX = 1;
	// 	this.vY = 1;
	// 	this.width = width;
	// 	this.height = height;

	// 	this.addChild(body);
	// };

	// Enemy.prototype.tick = function() {
	// 	this._x = this.x;
	// 	this._y = this.y;
	// 	this.y += this.vY;

	// 	if (this.y >= 300) { this.y = this._y; }
	// };

	// game.Enemy = Enemy;
})(game, createjs);
